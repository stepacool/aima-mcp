---
phase: 02-content-design
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/landing/how-it-works.tsx
autonomous: true
gap_closure: true
requirements:
  - HOW-04
must_haves:
  truths:
    - How It Works section has CTA at bottom
  artifacts:
    - path: frontend/components/landing/how-it-works.tsx
      provides: 4-step wizard flow + CTA button at bottom
      min_lines: 140
  key_links:
    - from: how-it-works.tsx
      to: Button component
      via: import from @/components/ui/button
      pattern: import.*Button
---

<objective>
Close gap HOW-04: Add CTA button at bottom of How It Works section.
</objective>

<execution_context>
@/Users/arterialist/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/components/landing/hero.tsx
@frontend/components/landing/how-it-works.tsx
@frontend/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Add CTA button to How It Works section</name>
  <files>frontend/components/landing/how-it-works.tsx</files>
  <action>
Add a "Start Building" CTA button at the bottom of the How It Works section (after the steps grid, before the closing </section> tag).

1. Import Button from @/components/ui/button (add to existing imports)
2. After line 131 (closing </motion.div> for steps grid), add:
   - A div with text-center mt-12 (or similar) to center the CTA
   - A "Start Building" primary button (variant="default", size="lg")
   - Button should have motion animation (fade in) on scroll like other elements
   
Reference: hero.tsx lines 41-51 for similar CTA button styling
  </action>
  <verify>npm run typecheck in frontend/ passes without errors</verify>
  <done>How It Works section now has "Start Building" CTA button at bottom - verified by seeing button element in rendered section</done>
</task>

</tasks>

<verification>
- [ ] CTA button "Start Building" appears at bottom of How It Works section
- [ ] Button has primary styling (matches hero CTA)
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
Gap HOW-04 closed: "How It Works section has CTA at bottom" now true
</success_criteria>

<output>
After completion, create .planning/phases/02-content-design/02-05-SUMMARY.md
</output>
