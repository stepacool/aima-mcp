---
phase: 02-content-design
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - frontend/app/(marketing)/page.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06
  - DESIGN-07
must_haves:
  truths:
    - Landing page composes all 6 sections in correct order
    - Design follows Vercel/Linear aesthetic with dark mode
    - All sections have proper spacing, heading hierarchy, animations, and accessibility
    - Mobile layout works correctly with 44px touch targets and responsive grids
  artifacts:
    - path: frontend/app/(marketing)/page.tsx
      provides: Composed landing page with all sections
      min_lines: 50
  key_links:
    - from: page.tsx
      to: components/landing/
      via: import
      pattern: from "@/components/landing/
---

<objective>
Compose all landing page sections into the main page and apply design polish.
</objective>

<execution_context>
@/Users/arterialist/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-content-design/02-RESEARCH.md
@frontend/app/(marketing)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Compose all sections in page.tsx and verify design consistency</name>
  <files>frontend/app/(marketing)/page.tsx</files>
  <action>
Update frontend/app/(marketing)/page.tsx to:

1. Replace existing sections with new landing sections in this order:
   - HeroSection → Hero (new)
   - FeaturesSection → How It Works (new)
   - Use Cases (new)
   - For Developers (new)
   - Comparison (new)
   - Trust (new)

2. Import new components:
   ```typescript
   import { Hero } from "@/components/landing/hero";
   import { HowItWorks } from "@/components/landing/how-it-works";
   import { UseCases } from "@/components/landing/use-cases";
   import { ForDevelopers } from "@/components/landing/for-developers";
   import { Comparison } from "@/components/landing/comparison";
   import { Trust } from "@/components/landing/trust";
   ```

3. Render sections in page:
   ```tsx
   <Hero />
   <HowItWorks />
   <UseCases />
   <ForDevelopers />
   <Comparison />
   <Trust />
   ```

4. Design consistency checks:
   - Vercel/Linear aesthetic: Use Geist font (already configured), minimal design
   - Dark mode: All sections use --marketing-bg, --marketing-fg CSS variables
   - Spacing: Consistent container padding (py-16 md:py-24)
   - Heading hierarchy: h2 for section titles (text-3xl md:text-4xl font-bold)
   - Accessibility: Ensure all interactive elements have focus-visible states
   - Focus states: Use buttonVariants which includes focus-visible ring
   - Animations: motion whileInView with consistent settings (viewport={{ once: true }})

5. Mobile responsiveness:
   - Minimum 44px touch targets
   - Responsive grids: 1 col mobile, 2 tablet, 3-4 desktop
   - text sizes: text-3xl md:text-4xl lg:text-5xl

6. Keep existing SEO/JSON-LD (from Phase 1)

7. Ensure build passes: npm run build
  </action>
  <verify>Build passes, all sections render correctly, responsive layout works</verify>
  <done>Landing page composes all 6 sections (Hero, How It Works, Use Cases, For Developers, Comparison, Trust) in correct order - Vercel/Linear aesthetic, dark mode, proper spacing, heading hierarchy, accessible colors, visible focus states, scroll animations, mobile responsive with 44px touch targets</done>
</task>

<task type="auto">
  <name>Verify all design requirements are met</name>
  <files>frontend/components/landing/</files>
  <action>
Final verification of design requirements across all sections:

1. DESIGN-01 (Vercel/Linear aesthetic): Check all sections use consistent minimal design, Geist font, clean spacing

2. DESIGN-02 (Dark mode): Test by toggling theme - all sections must use CSS variables, no hardcoded colors

3. DESIGN-03 (Spacing): Verify consistent section padding and container max-widths

4. DESIGN-04 (Heading hierarchy): Ensure h2 for sections, h3 for subsections, consistent text sizes

5. DESIGN-05 (Accessibility): 
   - Check color contrast (text-marketing-fg on bg-marketing-bg)
   - Ensure aria-labels on icon-only buttons
   - Verify focus indicators visible

6. DESIGN-06 (Focus states): 
   - Test Tab navigation through all interactive elements
   - Verify focus-visible ring appears

7. DESIGN-07 (Animations):
   - Test scroll-triggered animations work
   - Verify animations are smooth (transform/opacity only, no layout shift)
   - Check viewport settings (once: true, margin: "-100px")

Run: npm run build && npm run lint
  </action>
  <verify>npm run build passes, npm run lint passes</verify>
  <done>All 7 design requirements verified: Vercel/Linear aesthetic, dark mode, spacing, heading hierarchy, accessibility, focus states, animations all working correctly</done>
</task>

</tasks>

<verification>
- [ ] All sections composed in correct order
- [ ] Build passes without errors
- [ ] Lint passes without errors
- [ ] All 7 design requirements met
- [ ] Mobile responsive layout works
- [ ] Dark mode works on all sections
</verification>

<success_criteria>
Page assembly complete with all sections, all design requirements met, build and lint passing.
</success_criteria>

<output>
After completion, create .planning/phases/02-content-design/02-04-SUMMARY.md
</output>
